{
  "name": "branchsense",
  "version": "1.0.0",
  "description": "BranchSense: AI-powered learning pathways",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build"
  }
}
